server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/echo_user
    username: bodya
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
