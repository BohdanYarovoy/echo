package com.echoteam.app.utils;

import com.echoteam.app.entities.User;
import com.echoteam.app.entities.UserRole;

import java.util.ArrayList;
import java.util.stream.Collectors;

public class PerformUser {
/*
    public static UserDTO mergeUserDTO(UserDTO defaultUser, UserDTO change) {
        User user = User.of(defaultUser);
        
        if (change.id() != null)
            user.setId(change.id());
        if (change.nickname() != null)
            user.setNickname(change.nickname());
        if (change.firstname() != null)
            user.setFirstname(change.firstname());
        if (change.lastname() != null)
            user.setLastname(change.lastname());
        if (change.patronymic() != null)
            user.setPatronymic(change.patronymic());
        if (change.sex() != null)
            user.setSex(change.sex());
        if (change.email() != null)
            user.setEmail(change.email());
        if (change.password() != null)
            user.setPassword(change.password());
        if (change.dateOfBirth() != null) 
            user.setDateOfBirth(change.dateOfBirth());
        if (change.created() != null)
            user.setCreated(change.created());
        if (change.changed() != null)
            user.setChanged(change.changed());
        if (change.roles() != null) {
            ArrayList<UserRole> roles = change.roles().stream()
                    .map(UserRole::of)
                    .collect(Collectors.toCollection(ArrayList::new));
            user.setRoles(roles);
        }
        return user.toDTO();
    }


 */
}
